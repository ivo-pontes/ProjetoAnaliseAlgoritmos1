#include <stdio.h>
#include <stdlib.h>
#include "functions.h"
#include <time.h>

int main()
{
	/*
	clock_t start, end;
	double cpu_time_used;

	start = clock();
		createAllFiles();
	end = clock();
	cpu_time_used = ((double) (end - start)) / CLOCKS_PER_SEC;

	printf("Tempo de uso do CPU: %.2f.\n", cpu_time_used);
	*/

	menu();

	return(0);
}
